%h1 Editing truth
%br

= form_for(@truth) do |f|
  = render 'form', {:f => f}
  = f.text_field :votes
  #form_button.fieldset
    = f.submit

= link_to 'Delete Truth', truth_path(:id => params[:id]), :confirm => "Are you sure you want to delete #{@event}?", :method => :delete

= link_to 'Show', @truth |
= link_to 'Back', truths_path